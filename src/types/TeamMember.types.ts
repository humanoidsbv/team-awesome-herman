export interface TeamMemberProps {
  client: string;
  emailAddress: string;
  firstName: string;
  function: string;
  id: number;
  label: string;
  lastName: string;
  role: string;
  sortOption: string;
  startingDate: string;
}
