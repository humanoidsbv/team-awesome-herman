export interface TeamMemberProps {
  firstName: string;
  lastName: string;
  id: number;
  emailAddress: string;
  function: string;
  label: string;
  employer: string;
  role: string;
  startingDate: string;
}
