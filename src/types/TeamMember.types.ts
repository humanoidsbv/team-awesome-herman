export interface TeamMemberProps {
  emailAddress: string;
  client: string;
  firstName: string;
  function: string;
  id: number;
  label: string;
  lastName: string;
  role: string;
  startingDate: string;
  sortOption: string;
}
