export interface TeamMemberProps {
  emailAddress: string;
  employer: string;
  firstName: string;
  function: string;
  id: number;
  label: string;
  lastName: string;
  role: string;
  startingDate: string;
}
